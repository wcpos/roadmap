# 1.10.24 patch train — scope and release notes draft (prep, 2026-09-23)

Scope is everything on each `main` since the 2026-09-19 train (app 1.10.23, plugins 1.10.19, desktop 1.10.26, bundle v1.10.33), per the batch-the-patch-release rule.

## What is on `main` since the last train

| Repo | PRs | Merchant-visible? |
|---|---|---|
| monorepo | #2198 checkout acks, #2199 search index rebuild, #2204 translations pin 2026.9.12, #2191 docs note | #2198, #2199 |
| free | #2079 no-payment gateway status, #2073 refund save arms the order group, #2071 service-groups refactor (no behaviour change), #2063 namespace-slot stamping, #2065 readme product name, translations pin, test matrix/deps | #2079, #2073 |
| pro | #587 test expectation (unblocks main), translations pin, test matrix | none of its own |
| electron | translations pin 2026.9.12 | none of its own |

Not merged, so not in this train: monorepo #2082 (coupon search), #1581 (dead session blocks connecting); electron #318/#319.

## Versions

App **1.10.24**, bundle **v1.10.34**, free **1.10.20**, Pro **1.10.20**, desktop **1.10.27**, mobile **OTA**.

## Order

`apps/main/public/opfs.worker.js` did **not** move (the search fix is main-thread `packages/database/src/plugins/search.ts`), so no worker re-vendor and the spine's normal order holds:

monorepo bump #2205 (admin-merged, `aa8a3d34`) → bundle build → tag web-bundle v1.10.34 → app release v1.10.24 → OTA → free 1.10.20 (**Paul publishes the draft**) → Pro 1.10.20 → electron 1.10.27.

**Gate evidence:** bundle and OTA dispatched with the override. Justified: push Deploy on `16f1729e` (contains both fixes) is green including all four E2E shards; the only delta from there to the release merge is the app version and the translations constant (`git diff --stat 16f1729e aa8a3d34`: 2 files, 2 lines). D1: merge tree == bump tree == `66dde0fe…`.

**Free's PHP Tests on its tip must be green before the free bump merges** — merging cancels the in-flight run (lesson from the 1.10.19 train), and free carries the one money-path change (#2079).

## Translations

No leg. All four pins already at `2026.9.12`, the newest `wcpos/translations` tag, and no open translations PR. Free #2079 adds one new string (an order note: *"Order status set by %s; no payment was taken at the till."*); it ships in English until the next translations run.

## Mobile lane

`eas fingerprint:compare` from `apps/main` at the release tree, `EAS_BUILD_PROFILE=production`:

| Platform | Shipped build | Fingerprint | Result |
|---|---|---|---|
| Android | `247b12c8-4d98-47d2-8f3e-9840666d211d` | `050c0144c106f24ab23db6509c37f998f4c1dddd` | match |
| iOS | `d650ce31-d233-481f-99dc-edc037e58fab` | `612a275dcc877f7d400f203cd56c0b8f77490683` | match |

Both match → `publish-mobile-update.yml`, no store build.

## Draft release body — app v1.10.24

- **Checkout no longer reports "Checkout failed" for a sale that reached the store.** If the till was busy sending another change, or an earlier attempt had failed, the checkout screen could give up waiting even though the order had been saved. It now hears back as soon as the order is saved, and pressing Checkout again after a failure tries straight away.
- **Search repairs a damaged search index instead of failing at every launch.** When the saved search index could not be read, search stayed broken with "Failed to initialize search". The index is now discarded and rebuilt from the till's records.

(OTA paragraph added only after `eas update:list` confirms both platform groups.)

## Draft desktop body — electron 1.10.27

The two app lines. No desktop-only change this cycle.

## Draft `readme.txt` entry — free 1.10.20 (Pro `CHANGELOG.md` mirrors it)

= 1.10.20 - 2026/09/23 =

- **Checkout no longer reports "Checkout failed" for a sale that reached the store.** If the till was busy sending another change, or an earlier attempt had failed, the checkout screen could give up waiting even though the order had been saved. It now hears back as soon as the order is saved, and pressing Checkout again after a failure tries straight away.
- **Search repairs a damaged search index instead of failing at every launch.** When the saved search index could not be read, search stayed broken with "Failed to initialize search". The index is now discarded and rebuilt from the till's records.
- **Gateways that take no payment at the till — quotes, invoices, purchase orders — now close the order with the Order Status you set for them.** The checkout used to succeed but leave the order at POS - Open. Only gateways with an Order Status saved in POS > Settings > Checkout are affected, and the order is not marked paid.
- **Refunds that arrive from a payment gateway now respect your POS customer-email settings.** A refund recorded by a gateway's webhook could email the customer even with POS customer emails switched off; refunds made in wp-admin or at the till were not affected.

No Upgrade Notice (patch). Pro: free's four lines plus "(Bundles free plugin 1.10.20. No Pro-only changes this cycle.)"

## Provenance per bullet (merchants are on app 1.10.23, plugins 1.10.19, desktop 1.10.26)

| Bullet | Evidence it happened on the shipped version |
|---|---|
| Checkout failed on a saved sale | #2198: production Sentry on `wcpos-app@1.10.23` — "Checkout failed" is the top merchant-facing error, 129 events / 31 installs, ~70% `Timed out waiting for mutation`, breadcrumbs showing the sale reached the server. Strong. |
| Search index broken at every launch | #2199: desktop Sentry `WCPOS@1.10.26`, five Windows installs failing order search at every launch for days. Code is shared `packages/database`, so every platform. Strong. |
| No-payment gateway stays POS - Open | #2079: merchant report with Quotes for WooCommerce on the shipped plugin; reproduced live over HTTP. Strong. |
| Gateway refund ignores email setting | #2073: the lazy order group (`Init.php`, present at tag v1.10.19) is deferred on the storefront lane, where `?wc-api=` webhooks land; measured the email decision running before the group armed. Strong. |

**Struck:** #2071 service-groups refactor (no behaviour change); #2063 namespace-slot stamping (follow-up to the 1.10.19 registry work; no first-party controller has the affected shape, so invisible); #2065 readme wording; Pro #587 (test expectation only); translations pins; test-matrix and dependency chores.

## Risk read

- **#2198 is on the checkout path** and changes coalescing and backoff in the write queue. Fails toward sending sooner, not dropping: the ack is emitted only after durable removal. Mutation-checked tests; Deploy E2E green on the merge.
- **#2079 changes order status on the POS money path**, deliberately narrow (`pos-open` only, no `date_paid`, only a stored per-gateway status). 4791-test suite green locally and live-verified; full CI matrix is the gate before the free merge.
- #2199 only ever removes `*_flexsearch` collections; source collections are never touched.
