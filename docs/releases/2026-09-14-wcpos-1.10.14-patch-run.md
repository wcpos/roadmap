# 1.10.14 patch train — run record

Started 2026-09-14 ~17:00 UTC, on Paul's request to release today's fixes. Everything on the four `main` trunks was already merged; no work in flight.

## Version lines

| Line | Released | This train | PR |
|---|---|---|---|
| Monorepo app | v1.10.13 | **v1.10.14** | [monorepo#2048](https://github.com/wcpos/monorepo/pull/2048) |
| Web bundle | v1.10.23 | **v1.10.24** | tag only |
| Electron desktop | 1.10.16 | **1.10.17** | [electron#443](https://github.com/wcpos/electron/pull/443) |
| Free plugin | 1.10.13 | **1.10.14** | [woocommerce-pos#1986](https://github.com/wcpos/woocommerce-pos/pull/1986) |
| Pro plugin | 1.10.13 | **1.10.14** | [woocommerce-pos-pro#571](https://github.com/wcpos/woocommerce-pos-pro/pull/571) |
| Translations pin | 2026.9.10 | unchanged | no open translation PRs, no new tag |

## What rides (diff against the last released tags)

Monorepo `v1.10.13...main`: #2033 receipt template display order, #2037 phrase search (client half), #2044 search index lifecycle, #2039 test only, two `docs(context)`.
Free `v1.10.13...main`: #1979 order-email route schema, #1980 phrase search (server half), #1983 pot, #1984 + #1985 release recovery (CI only).
Pro `v1.10.13...main`: #569 test bootstrap, #570 release recovery, two dependency bumps. No Pro-only merchant change.
Electron: nothing since 1.10.16; the bump exists so desktop users receive the app fixes.

**`apps/main/public/opfs.worker.js` did not change** — sha256 `899a6bdf…` on the monorepo tip equals free's vendored copy — so free needs no re-vendor and free does not have to wait for the bundle tag for correctness. It is still held until the tag exists so that its changelog's app lines are live for web merchants at publish time.

Mobile: not dispatched. The last production `build.yml` run was 2026-09-10 (app 1.10.10); the 1.10.11, 1.10.12 and 1.10.13 trains did not submit either. Left as a question for Paul.

## Sequence

1. Merge monorepo #2048 once ✅ Merge Gate is green.
2. Wait for the push Deploy on the merge SHA, then `Publish Web Bundle` with `monorepo_ref` = merge SHA, `bundle_branch` = `main`.
3. Tag web-bundle `v1.10.24` at the new bundle commit (full SHA), purge jsDelivr, confirm `@1.10` serves `x-jsd-version: 1.10.24`.
4. `gh release create v1.10.14` on wcpos/monorepo at the full merge SHA, `--latest`, with the app notes.
5. Electron #443 out of draft → merge → cancel the push Publish run → dispatch `tag-and-release.yml` `platform=all` → publish the draft once assets land.
6. Free #1986 out of draft → merge → `release.yml` cuts the draft → **Paul publishes** → verify WP.org SVN `Stable tag`.
7. Pro #571 out of draft → merge → `manual-release.yml` `releaseVersion=1.10.14` → verify the zip vendors free 1.10.14.

## Log

- 17:0x UTC — four worktrees created from `origin/main`; markers bumped; PRs opened. Monorepo #2048 ready, the other three drafts.
- 17:5x UTC — monorepo #2048 Merge Gate green, merged at `58c5c636`. Push Deploy run 34874307576 green (Deploy + E2E).
- 18:17 UTC — `Publish Web Bundle` run 34879938491 from `58c5c636` → web-bundle `main` at `d53767a7` (entry names 1.10.14; worker blob `5c46059e` identical to the monorepo tip). Tagged **v1.10.24**, jsDelivr purged, `@1.10` serves `x-jsd-version: 1.10.24`.
- 18:2x UTC — app release **v1.10.14** created at the full merge SHA, `--latest`.
- 18:2x UTC — electron #443 readied, drift rerun green, merged at `dea7d806`; push Publish run 34880536031 cancelled; `tag-and-release.yml platform=all` dispatched (run 34880608446).
- 18:2x UTC — free #1986 merged at `50e270ec`; `release.yml` run 34880505608 built draft **v1.10.14** (zip 8,070,300 bytes, release id 388617091); body set by PATCH with `tag_name` preserved. **Awaiting Paul's publish.**
- 18:24 UTC — Pro #571 merged at `de9b24e6`; `manual-release.yml releaseVersion=1.10.14` dispatched (run 34880642431).
- 18:3x UTC — **Pro 1.10.14 published** by `manual-release.yml` (run 34880642431); body is the CHANGELOG top section. Shipped zip verified: Pro markers 1.10.14, vendored free 1.10.14 (`Stable tag: 1.10.14`), translations 2026.9.10, worker sha256 `899a6bdf…` (unchanged since 1.10.13, as expected).
- 18:39 UTC — **Paul published free v1.10.14** (tag at `50e270ec`). `wporg-deploy.yml` run 34882134982 green; verified by SVN: trunk `Stable tag: 1.10.14`, `tags/1.10.14/` HTTP 200, tagged plugin header `Version: 1.10.14`.
- 18:43 UTC — **Desktop 1.10.17 published**: Publish run 34880608446 green on all four platforms (no reruns), 9 assets, `release-notes` job footered "Desktop build of WCPOS app **1.10.14**" correctly; git tag created at the merge SHA `dea7d806` before publishing.

## TRAIN COMPLETE — all five artifacts shipped and verified

| Artifact | Version | Verified by |
|---|---|---|
| Web bundle | **v1.10.24** | tagged at `d53767a7` from monorepo `58c5c636`; jsDelivr `@1.10` → `x-jsd-version: 1.10.24`; entry names 1.10.14 |
| App | **v1.10.14** | release at the full merge SHA, `--latest` |
| Desktop | **1.10.17** | published 18:43 UTC, 9 assets, tag at `dea7d806`, footer reads app 1.10.14 |
| Free plugin | **1.10.14** | published by Paul 18:39 UTC; WP.org SVN `Stable tag: 1.10.14`, `tags/1.10.14/` 200 |
| Pro plugin | **1.10.14** | zip vendors free 1.10.14, worker `899a6bdf…`, translations 2026.9.10 |

Mobile: shipped on Paul's say-so after the five legs above. `build.yml platform=all profile=production submit=true` run 34893748942 on `58c5c636` (20:34–21:19 UTC, gate passed first time). Android build `94b8dee6`, version code 26, submitted to Google Play; iOS build `668f0de9` uploaded to App Store Connect (App ID 6742204061), now in Apple processing. Neither store's review/rollout state is verified here.

### Notes worth keeping

- **A train with no worker change needs no re-vendor and no ordering inversion.** The tag diff showed `apps/main/public/opfs.worker.js` untouched and the free copy's sha256 already matched the monorepo tip, so free's drift check stayed green through the v1.10.24 tag. Free was still held until the tag existed so its changelog's app lines were live for web merchants at publish.
- **Zero interventions.** Every gate went green first time; the only retries were `gh pr merge` refusing twice right after `gh pr ready` on free, which is the known transient.
- **Pro's `manual-release.yml` writes the CHANGELOG top section as the body itself** — no post-publish body overwrite was needed this time.
