# 2026-09-10 — WCPOS app 1.10.10 patch train (search walk fix) — run record

Status: **complete** (11:55 UTC) — web bundle, app release and desktop live; iOS/Android submitted and pending store review. Paul gave the go at 10:47 UTC once the PRs were open; the desktop leg was held mid-train for one extra electron PR (#422). Paul: *"There are some search engine fixes that I want to release as a patch release for all platforms."* Mobile production build + submit is in scope. **No plugin leg**: the plugin side of the search fix (AND-joined terms) shipped in woocommerce-pos 1.10.8; free/pro stay at 1.10.10.

Source handoff: `monorepo-v2/docs/releases/2026-09-10-app-1.10.10-search-walk-handoff.md` (local, `docs/` is gitignored there).

## Versions

| Line | Was | Now |
|---|---|---|
| Free plugin | 1.10.10 | 1.10.10 (unchanged) |
| Pro plugin | 1.10.10 | 1.10.10 (unchanged) |
| App (`apps/main`) | 1.10.9 | 1.10.10 |
| Web bundle | v1.10.19 | v1.10.20 (from the release merge SHA) |
| Desktop | 1.10.10 | 1.10.11 (first fully standalone build; also carries wcpos/electron#422 Cloudflare challenge fix: `.monorepo-ref` v1.10.9 → v1.10.10; ships app 1.10.10) |
| Translations | 2026.9.9 | 2026.9.9 (unchanged; bot PR wcpos/translations#309 left open, not part of this train) |

## What is in it

- Monorepo `v1.10.9..main` (tip `8986748`): wcpos/monorepo#1935 product search walk fix (no request loop past 100 results, one full page per request, resumes from the covered prefix on scroll, no 100-row cap); wcpos/monorepo#1936 + #1937 search fixture catalogue, scan-matcher extraction, walk-politeness and latency seam tests, grid footer counts rendered rows; wcpos/monorepo#1933 `apps/electron` submodule removed (desktop standalone, roadmap#177 phase 3).
- Electron `v1.10.10..main`: wcpos/electron#419 (IPC registry in-repo, nested workspace dropped) and #420 (`.monorepo-ref` pin + `monorepo-drift.yml`) — internal; the push-triggered Publish after #420 (run 34400865330) built the renderer from the v1.10.9 pin green on Linux.
- Production code touched by the fix: `packages/sync-engine` (product fetcher, require-plane, replication-policy), `packages/query` (search-match extraction), `packages/core` product grid footer count. No `pnpm-lock.yaml` change since v1.10.9 → no `onlyBuiltDependencies` gap for the strict bundle install. No `packages/database` change → OPFS worker expected unchanged (verify by hash against v1.10.19 before deciding on a re-vendor).
- No schema, storage or migration change. No native code change; mobile still needs a store build because there is no OTA channel.

## Release notes (app release body; electron's `release-notes` job copies it onto the desktop draft)

- **Product search no longer loops or crawls on large catalogues.** A search with more than 100 matching products could re-request the same pages over and over until the page was reloaded, and every scroll re-fetched the results from the start.
- **Search results are no longer capped at 100 products.** Scrolling loads the rest, one page at a time.
- **Product search fills the grid faster on slow hosting.** One request per page replaces the previous escalating sequence of four.

Struck: grid footer rendered-row count (invisible outside sync churn; an E2E referent), electron submodule removal and IPC refactor (plumbing).

## Pre-flight

- Handoff SHA `8986748`: Test green; Deploy still in progress at 10:44 UTC (the previous head `084b71e`, the fix itself, is green on Test, Deploy and E2E Native).
- No stale drafts: monorepo latest release v1.10.9, electron latest v1.10.10, web-bundle tip `4c2dae3f` = v1.10.19.
- Monorepo release worktree `~/Projects/monorepo-v2-worktrees/release-1.10.10`; electron worktree `~/Projects/electron-worktrees/release-1.10.11`. Submodule tripwire clean (`--show-toplevel` is the worktree; `apps/web` gitlink moved with `update-index --cacheinfo`, no submodule checkout).
- Roadmap main tree was 4 commits behind and blocked by untracked `docs/prototypes/`; moved aside to the session scratchpad (`backup-untracked/`), pulled. Four seasonal-idle prototype HTML files differed from the committed versions (local edits 2026-09-09 20:10, a few lines each) — kept in the backup, not restored over the tracked files.

## PRs

| Repo | PR | Branch | State at open |
|---|---|---|---|
| wcpos/monorepo | #1938 | `release/1.10.10` | ready (`apps/main` 1.10.10, `apps/web` → `4c2dae3f` = v1.10.19 reference pin) |
| wcpos/electron | #421 | `release/1.10.11` | draft; rebased onto #422 mid-train, then ready → merged `998adaca` |

## Sequence

1. Merge monorepo #1938 → Publish Web Bundle from the merge SHA (override the main-verification gate only if main's Deploy for that SHA is still running: pure bump on a green tip) → verify by SHA (entry hash new, worker hash vs v1.10.19, app 1.10.10 present) → **tag `v1.10.20` (held)** → purge jsDelivr → confirm `@1.10` serves 1.10.20.
2. **App release `v1.10.10` on wcpos/monorepo (held)** at the full merge SHA with the notes above; `--latest`.
3. Mark electron #421 ready → merge → cancel the push-triggered Publish → dispatch `tag-and-release.yml platform=all` → draft → check the body was copied from the v1.10.10 app release → git tag `v1.10.11` at the full SHA → **publish (held)**.
4. **Mobile production build + submit (held)**: `build.yml platform=all profile=production submit=true` from main once at 1.10.10 (same gate override rule).

## Progress log (UTC)
- 10:3x wcpos/monorepo#1938 opened (`e94fa0e2`), Merge Gate running. wcpos/electron#421 opened as draft (`331116d`).
- 10:47 Paul: go for bundle tag, app release, desktop and mobile. Then: *"don't release the Electron app yet. There's going to be one more PR"* — desktop leg held; #421 body updated, to be rebased on that PR.
- 11:04 Merge Gate pass → wcpos/monorepo#1938 merged (`f89daaf7`). main Deploy for f89daaf running; 8986748 Deploy green. Publish Web Bundle dispatched (monorepo_ref = f89daaf7, bundle_branch = main, gate override: pure bump on a green tip). Mobile build.yml platform=all profile=production submit=true dispatched from main (same override).
- 11:05 app release `v1.10.10` created on wcpos/monorepo at `f89daaf7` (latest; notes = the three lines above). Bundle run 34469387260 and mobile run 34469390248 in progress.
- 11:09 Publish Web Bundle run 34469387260 green → web-bundle main `b30b001d` from monorepo `f89daaf7`; verified by SHA (entry `entry-06963f47…` vs v1.10.19 `entry-27820bf3…`; opfs/indexeddb workers byte-identical to v1.10.19 → no re-vendor; app 1.10.10 present).
- 11:11 web-bundle **tagged `v1.10.20`** at `b30b001d`; jsDelivr purged; `@1.10` serves `x-jsd-version: 1.10.20`, entry `entry-06963f47…`. **Web app update live.** Desktop held (Paul's extra electron PR pending); mobile run 34469390248 still building.
- 11:26 Paul: *"electron has merged"* — wcpos/electron#422 (Cloudflare challenge clearer; desktop-only, no lock change) on main `1f21b58`. `release/1.10.11` rebased onto it (`a845547`), pushed, #421 marked ready; waiting on its `test` check before merging. Desktop body prepared: app notes + Cloudflare line.
- 11:29 electron #421 `test` pass → merged (`998adaca`); push-triggered Publish 34471488355 cancelled; `tag-and-release.yml platform=all` dispatched → run 34471531758. Tag `v1.10.11` + publish held until the draft has all 9 assets.
- 11:35 electron draft v1.10.11 (id 386226770) appeared with 5 assets and an empty body (release-notes job had not written yet); body + name set WITH tag_name (tag survived). Waiting for the run to finish (9 assets expected).
- 11:47 electron Publish run 34471531758 green (all 6 jobs); draft had the same 9 assets as 1.10.10, body intact. Git tag `v1.10.11` at `998adaca`; draft **published**, latest. **Desktop live** (app 1.10.10 + Cloudflare challenge fix).
- 11:55 mobile build.yml run 34469390248 green: Android build f2015d0b + iOS build bff3129e finished 11:52; "All submissions have finished" 11:55 (Android submission 5a8a0d85, iOS submission 12423770). **iOS/Android submitted**, pending store review. Train complete.

## Follow-ups

- The next free/Pro changelog entry must carry the three search lines above (plugin notes cover the whole train; no plugin release this time).
- wcpos/translations#309 (rolling bot PR, open since 2026-09-09) was left alone.
- Desktop notes are hand-written on the draft: the electron `release-notes` job had not written by the time the draft appeared, and the Cloudflare line is desktop-only anyway.
