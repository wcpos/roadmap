# 1.10.24 mobile OTA-only leg — run record (2026-09-23, afternoon)

Paul's call (2026-09-23 12:xx local): "OTA only" for monorepo#2208, after being offered the full 1.10.25 train, OTA only, or merge only. No version bump, no web bundle, no desktop, no plugin legs: nothing else had landed on `main` since the morning's 1.10.24 train (`aa8a3d34`).

## What shipped

| Leg | Version | Key SHA / ref | Verified by |
|---|---|---|---|
| App (monorepo) | still `1.10.24` | merge `5f702dfc6bf914d1d5ee505ac8d13529bbe59701` (PR #2208) | merge tree `69b41c4d…` == PR tip `acc142481` tree |
| Mobile | OTA on `production` | `publish-mobile-update.yml` run 35860215824 at `5f702dfc` | `update:list --branch production`: iOS group `e774b0b0-b8b0-45d0-880e-f663e717be70` rt `612a275d…`, Android group `46cc4b4f-8111-45ac-9253-6ea0bb6504d0` rt `050c0144…`, both "app v1.10.24 5f702dfc" |

## What rides

monorepo#2208: BLE discovery waits for the adapter state before scanning (the iOS first-scan "BluetoothLE is in unknown state" double failure); Sentry `beforeSend` adds a scrubbed `contexts.thrown` for message-less exceptions. Prompted by TestFlight crash feedback 2026-09-20 (Sentry WOOCOMMERCE-POS-2S5). The crash's thrower is still unidentified; the diagnostics ride so the next occurrence names it.

## Checklist boxes

- A4: `eas fingerprint:compare --build-id` at the PR tip tree — iOS `d650ce31…` matches `612a275d…`, Android `247b12c8…` matches `050c0144…`. Same → OTA.
- D1: merge-commit tree == PR-tip tree (`69b41c4d6562ec73abd207e1261b9c69eb2e7600`), so A4 stands for the merge SHA.
- E2: dispatched from the merge SHA with the override; justification: Deploy on the identical tree (`acc142481`, run 35855939408) green incl. all four E2E shards, and main's own Deploy at `5f702dfc` (run 35857865810) went green before the publish finished. Both groups' runtime versions equal the shipped builds'.
- B and F do not apply: no version bump, no release notes, no GitHub release. Web (`@1.10` → 1.10.34) and desktop (1.10.27) do not carry #2208 until the next train.

## Things worth knowing

- The PR-tip native E2E showed as red on the merged PR; it was **cancelled by "Cancel native E2E runs of a closed PR"** at merge time, not a failure. Main's E2E Native at `5f702dfc` (run 35857865856) is the real signal; it was still running when the OTA published.
- Codex's GitHub connector and CodeRabbit each raised findings on the first push (state-snapshot race, unscrubbed `thrown` fields, budget comment); all six threads were fixed and resolved before merge.
