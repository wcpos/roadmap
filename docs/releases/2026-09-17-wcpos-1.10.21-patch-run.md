# 1.10.21 patch train — run record

Started 2026-09-16 ~23:05 UTC on Paul's go ("Okay can you start the release train then?") after the scope/notes prep in `2026-09-17-wcpos-1.10.21-patch-train-prep.md`. Seven merges landed on monorepo `main` after the 1.10.20 release SHA `5bb4577d`; two of them (#2114, #2119) rebuilt `apps/main/public/opfs.worker.js`, so the plugin legs are required and the tag order inverts (bundle → vendor → tag → free). Free and Pro have no product commits of their own since v1.10.16.

## Version lines

| Line | Released | This train | PR |
|---|---|---|---|
| Monorepo app | v1.10.20 | **v1.10.21** | [monorepo#2121](https://github.com/wcpos/monorepo/pull/2121) |
| Web bundle | v1.10.30 | **v1.10.31** | tag only |
| Electron desktop | 1.10.23 | **1.10.24** | [electron#461](https://github.com/wcpos/electron/pull/461) |
| Free plugin | 1.10.16 | **1.10.17** | [woocommerce-pos#2005](https://github.com/wcpos/woocommerce-pos/pull/2005) |
| Pro plugin | 1.10.16 | **1.10.17** | [woocommerce-pos-pro#575](https://github.com/wcpos/woocommerce-pos-pro/pull/575) |
| iOS / Android | 1.10.18 store build + 1.10.20 OTA | **OTA update on `production`** | `publish-mobile-update.yml` at the merge SHA |
| Translations pin | 2026.9.10 | unchanged | latest tag is 2026.9.10 |

## What rides

Monorepo `v1.10.20...main` (28 commits, tip `a8590835` = #2120 merge, cut from there):

- [#2115](https://github.com/wcpos/monorepo/pull/2115) + [#2120](https://github.com/wcpos/monorepo/pull/2120) — boundary-caught render errors reported (CLIENT151, Logs row `render.error`); an incomplete store session is refused at login/switch and recovered at hydration with AUTH131 (toast + store list) instead of the red `useStoreSession` banner (#2112). Login-blocked tills: electron#459, Sentry 2M8.
- [#2114](https://github.com/wcpos/monorepo/pull/2114) + [#2119](https://github.com/wcpos/monorepo/pull/2119) — storage write repair-and-retry when rows moved after the preflight; every repair refusal reported (mirror of electron#460). **Rebuilds `opfs.worker.js`** (blob `3824bc10` → `12fad51b`).
- [#2113](https://github.com/wcpos/monorepo/pull/2113) — change-signal cursor primed at scope open; a fresh till no longer misses a stock change in its first minute.
- [#2116](https://github.com/wcpos/monorepo/pull/2116) — variation popover resolves parent-managed stock from the parent (badge + Add to Cart were stale up to 317 s).
- [#2118](https://github.com/wcpos/monorepo/pull/2118) — `X-WCPOS-Store` header follows the engine's scope activation; requests inside a store switch no longer carry the outgoing store.

Free `v1.10.16...main`: #2002 (changelog text), #2003 (release.yml body-from-readme) — no product change. Pro `v1.10.16...main`: #574 (changelog text). Electron `v1.10.23...main`: [#460](https://github.com/wcpos/electron/pull/460) (the storage fix, `.monorepo-ref` pinned to the #2119 merge SHA `306bc84d`; restored to a tag by #461).

## Release notes (app v1.10.21) — B1, file `app-notes.md`

- **A till no longer gets stuck at login on "Something went wrong: useStoreSession must be called within an active store session".** A damaged range in the till's local database made the login write fail and left the cashier on a red banner. The write is now repaired and retried, and if the saved session still cannot be honoured the till returns to the store list with a message instead of the banner. If the site has to be added again, it opens a fresh local database and does not pick up sales still waiting to sync in the old one. Web merchants receive the storage repair through WooCommerce POS plugin 1.10.17, which serves the storage worker; desktop and phone apps carry it in this release.
- **A newly connected till no longer misses a stock change made in its first minute.** A product set out of stock on the server shortly after a new device, login or reset stayed "in stock" on the till indefinitely. The till now records the server's position before the first browse, so the change arrives with the next sync.
- **A variation whose stock is managed at product level shows the right stock in the variation picker.** When the parent product sold out, the picker's badge and Add to Cart button kept saying "in stock" for up to five minutes. They now read the parent's stock, the same way the cart does.
- **Switching stores no longer carries the previous store into the first requests of the new one.** Requests made while a switch was still completing used the outgoing store, so the first products or barcodes could belong to it until the next sync. Pro multi-store only.
- **Health > Logs records a screen error the app catches** as "Part of the screen failed to load", and reports it when you have allowed error reporting.

Phone and tablet apps on the 1.10.18 store build receive this version as an over-the-air update: it downloads in the background and applies on the next cold start after that, with no store update needed. Older store versions must first update to 1.10.18 from the App Store or Google Play.

## Release notes (plugins 1.10.17) — B2

The five app bullets above, verbatim (B3: 5 of 5). No plugin-side lines this train. Pro's entry adds "(Bundles free plugin 1.10.17. No Pro-only changes this cycle.)". The desktop body is the five bullets plus the desktop footer, without the phone OTA paragraph.

## OTA decision (A4)

`EAS_BUILD_PROFILE=production npx eas-cli fingerprint:compare --build-id` from `apps/main` in a detached worktree at `a8590835` (`pnpm install --offline --frozen-lockfile`, exit 0). Note: plain `eas` / `npx eas` are not on this machine; `npx eas-cli` is the invocation.

| Platform | Shipped 1.10.18 build | Runtime version | Result |
|---|---|---|---|
| Android | `247b12c8-4d98-47d2-8f3e-9840666d211d` | `050c0144c106f24ab23db6509c37f998f4c1dddd` | ✅ matches local |
| iOS | `d650ce31-d233-481f-99dc-edc037e58fab` | `612a275dcc877f7d400f203cd56c0b8f77490683` | ✅ matches local |

## Checklist (RELEASE-CHECKLIST.md)

- [x] A1 tag diffs on all four lines (above)
- [x] A2 worker blob `3824bc10` (v1.10.20, and free `main`) → `12fad51b` (`a8590835`)
- [x] A3 plugins: required because the worker moved
- [x] A4 both platforms match the 1.10.18 store builds
- [x] A5 peers: kilbot-0a idle, journal-67 idle, monorepo-v2-11 idle, monorepo-v2-ea busy (not a train; #2120 was merged by Paul at 23:00Z)
- [x] B1 `app-notes.md` · B2 `plugin-notes.md` · B3 5/5
- [x] C1 `release/1.10.21` at `1649668b` (git-data API), #2121 — first queued on auto-merge, then **admin-merged on Paul's instruction** at `71a65925` (23:2x). C1 rewritten in the checklist: version bumps are admin-merged at once.
- [x] C2 free `release/1.10.17` at `2bd2749b` (package.json, readme.txt Stable tag + entry, woocommerce-pos.php header + VERSION), #2005 draft
- [x] C3 pro `release/1.10.17` at `52484d35` (package.json, CHANGELOG.md, woocommerce-pos-pro.php header + VERSION), #575 draft
- [x] C4 electron `release/1.10.24` at `f089b123` (package.json, `.monorepo-ref` = `v1.10.21`), #461 draft
- [x] D1 merge tree `b6be3959` == release-commit tree `b6be3959`; A4 stands
- [x] D2 `Publish Web Bundle` run 35161937140 from `71a65925` (+ override): web-bundle `d731fef5`, entry `_expo/static/js/web/entry-6036d9dda9a76c07f8070ee522b5cfbd.js`, worker blob `12fad51b` == monorepo
- [x] D3 worker vendored onto free `release/1.10.17` by blob (`b22ececc`): monorepo == bundle == free == `12fad51b`; free `OPFS Worker Drift` green on that commit
- [x] D4 tag `v1.10.31` at `d731fef5`; `@1.10` read 1.10.30 → purged `metadata.json` + `index.html` → `x-jsd-version: 1.10.31`; entry asset 200
- [x] D5 app release v1.10.21 at `71a65925`, latest, body = B1
- [x] D6 free #2005 readied + admin-merged at `ca573c86`; `release.yml` run 35162747434 built draft 390337603 with `target_commitish` = `ca573c86`; zip verified (flat; header 1.10.17, `Stable tag: 1.10.17`, `VERSION` 1.10.17, worker blob `12fad51b`, readme entry 5 bullets); body 5/5 B1 bullets. **Published by Paul** (~23:41Z), tag at `ca573c86`
- [x] D7 `wporg-deploy.yml` run 35163378707 green on `ca573c86`; SVN trunk `Stable tag: 1.10.17`, `tags/1.10.17/` 200, tagged header 1.10.17, worker bytes `12fad51b` in BOTH trunk and tag, tag readme entry 5 bullets
- [x] D8 Pro #575 readied + admin-merged at `2bcf0844`; `manual-release.yml releaseVersion=1.10.17` run 35162756134 green; v1.10.17 published; zip verified (flat; header + `const VERSION` 1.10.17, translations 2026.9.10, vendored free `Stable tag: 1.10.17`, worker blob `12fad51b`); body 5/5 + bundles line
- [x] E1 electron drift rerun (35161741154) green with the app tag; #461 readied + admin-merged at `f8e23615`; push Publish 35162665075 cancelled; `tag-and-release.yml platform=all` run 35162688429 green; draft 390339083 had 9 assets and the footer "Desktop build of WCPOS app **1.10.21**"; tag `v1.10.24` at `f8e23615`; published latest with the desktop body (bullets + footer, OTA paragraph removed) via PATCH with `tag_name`
- [x] E2 `publish-mobile-update.yml` run 35161956199 from `71a65925` (+ override) green; `eas update:list --branch production`: Android group `98fd97c2…` runtime `050c0144…`, iOS group `6685461c…` runtime `612a275d…`, message `app v1.10.21 71a65925`
- [x] F1 sync PR [#2123](https://github.com/wcpos/monorepo/pull/2123) `c6d98038` (API merge 409 on 14 files → worktree; Codex resolved 13 as unions + regenerated the logger/logs files with `generate-error-codes.mjs` / `generate-event-labels.mjs`; worker regenerated after an ONLINE install with `scripts/build-opfs-worker.mjs`, markers: document-repair-refused 1, write-retry-failed 1, hollow-row-refused 1, WcposChangelogOversized 2, TASK_QUEUE_CONTAINMENT 1, CLEANUP_COMPACTION_BATCH 1, EXACT_CLEANUP_DELETE 1; node --test recovery suites 0, check-event-labels 0, typecheck 15/15). Auto-merge queued behind its Merge Gate
- [x] F2 this record
- [x] F3 bodies: monorepo v1.10.21 2028, woocommerce-pos v1.10.17 1743, woocommerce-pos-pro v1.10.17 1802, electron v1.10.24 1861 — none empty

## Log (UTC)

- 23:00 — #2120 merged by Paul at `a8590835`; push Deploy run 35160326600 started.
- 23:0x — scoped from tag diffs; worker moved at #2114/#2119; plugins required. Notes written, B3 5/5.
- 23:16 — fingerprint compare at `a8590835`: Android `050c0144…` and iOS `612a275d…` match the 1.10.18 store builds.
- 23:18 — four release branches cut through the git-data API; #2121 ready + auto-merge, #2005 / #575 / #461 drafts.
- 23:2x — **Paul: "Need to be able to force merge, like just version bumps. There's absolutely no reason to wait for the tests."** #2121 admin-merged at `71a65925`. Trees equal. Bundle run 35161937140, OTA run 35161956199 dispatched with the override; app release v1.10.21 created, latest.
- 23:2x — electron #461 readied; drift rerun green with the app tag.
- 23:30 — bundle green: web-bundle `d731fef5`, worker `12fad51b`. Vendored onto free `release/1.10.17` (`b22ececc`). Tagged **v1.10.31**; `@1.10` read 1.10.30 → purged → 1.10.31; entry 200. **Web live.**
- 23:3x — electron #461 admin-merged at `f8e23615`; push Publish cancelled; `tag-and-release.yml platform=all` run 35162688429 dispatched.
- 23:3x — free #2005 admin-merged at `ca573c86` (drift green on the vendored commit); Pro #575 admin-merged at `2bcf0844`; `manual-release.yml 1.10.17` dispatched (35162756134).
- 23:3x — `main` → `next` API merge 409 (14 files); worktree `sync-1.10.21`; handed to Codex (`codex exec -m gpt-6-astra`, task in a file). Codex resolved 13 files but could not rebuild the worker (offline store lacked `turbo-2.10.11.tgz` for next's lockfile) or stage (sandbox cannot write the worktree index). Finished on the Claude side: online install, worker rebuild, checks, commit, push.
- 23:35 — OTA run green; both production groups at `app v1.10.21 71a65925`. **Mobile live over the air.**
- 23:3x — free draft verified (target `ca573c86`, zip flat, worker `12fad51b`, 5 bullets) and handed to Paul.
- 23:38 — Pro run green; **Pro v1.10.17 published** and zip verified.
- 23:40 — sync #2123 opened, auto-merge queued behind its Merge Gate.
- 23:41 — **Paul published free v1.10.17**; `wporg-deploy.yml` 35163378707 green; SVN verified (trunk + tag worker `12fad51b`, 5 bullets). **Free live on WP.org.**
- 23:5x — electron run green: 9 assets, footer 1.10.21. Tag `v1.10.24` at `f8e23615`; published latest with the desktop body. **Desktop live.** F3 bodies all non-empty.

## TRAIN COMPLETE — six artifacts shipped and verified; sync queued

| Artifact | Version | Verified by |
|---|---|---|
| App | **v1.10.21** | GitHub release at `71a65925`, latest; #2121 admin-merged (version bump) |
| Web bundle | **v1.10.31** | `@1.10` serves `x-jsd-version: 1.10.31`, entry asset 200; worker blob `12fad51b` == monorepo |
| Free plugin | **1.10.17** | published by Paul at tag `ca573c86`; SVN trunk `Stable tag: 1.10.17`, `tags/1.10.17/` 200, worker bytes == bundle in trunk and tag |
| Pro plugin | **1.10.17** | `manual-release.yml` run 35162756134; zip vendors free 1.10.17, worker `12fad51b`, translations 2026.9.10 |
| Electron | **1.10.24** | run 35162688429 green on all platforms, 9 assets, published at `f8e23615`, latest |
| iOS / Android OTA | **app v1.10.21 on `production`** | groups `98fd97c2…` (Android, runtime `050c0144…`) and `6685461c…` (iOS, runtime `612a275d…`) match the 1.10.18 store builds |
| `main` → `next` | #2123 | auto-merge queued behind its Merge Gate at `c6d98038` (worker regenerated, marker-proved) |

### Notes worth keeping

- **Version-bump release PRs are admin-merged the moment they open.** Paul's ruling this train, now checklist C1 and roadmap memory `feedback-admin-merge-version-bump-prs`. Waiting on the PR's own gate cost ~10 minutes for nothing.
- **`eas` is not on PATH; `npx eas-cli` is.** `npx eas` fails with "could not determine executable to run" and reads like a fingerprint failure.
- **Codex cannot finish the `next` sync on its own.** Two hard stops in the sandbox: next's lockfile needs packages the offline store lacks (online `pnpm install` required), and the worktree's git index lives in the parent repo outside the sandbox (`index.lock: operation not permitted`). Use Codex for the conflict resolution and do install / worker rebuild / stage / commit / push from the Claude side.
- **The electron `release-notes` job copies the app body including the phone OTA paragraph.** Strip it when publishing the desktop draft (PATCH with `tag_name`, `target_commitish`, `draft:false`).
- **Free's `OPFS Worker Drift` check compares against the highest `v1.10.x` bundle tag**, so it can only go green once the new tag exists. This train's vendor push and tag were seconds apart and the check queued long enough to see the tag; do not rely on that — tag, then look at drift.
