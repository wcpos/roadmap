# 2026-09-06 — WCPOS 1.10.8 patch train (run record)

Status: **shipped, all legs** (10:57 UTC). Paul asked for a patch release plus a web build address and an Electron dev build; mid-train he waived the check (*"you can publish when ready"*, *"there is no fucking time left"*) and added iOS/Android production.

## Versions this train

| Line | Was | Now |
|---|---|---|
| Free plugin (`wcpos/woocommerce-pos`) | 1.10.7 | **1.10.8** — PR #1889, live on WP.org 10:04 UTC |
| Pro plugin (`wcpos/woocommerce-pos-pro`) | 1.10.7 | **1.10.8** — PR #533, published 09:58 UTC |
| App (`wcpos/monorepo`, `apps/main`) | 1.10.6 | **1.10.7** — PR #1890, release created 10:06 UTC |
| Web bundle (`wcpos/web-bundle`) | v1.10.16 | **v1.10.17** live on jsDelivr `@1.10` (from monorepo `3387122a`) |
| Desktop (`wcpos/electron`) | 1.10.7 | **1.10.8** — PR #412, published 10:19 UTC |
| Translations | 2026.9.7 | **pending** — the monorepo tip pushed new JS strings at 08:56 UTC; the Aide/OpenClaw translation task failed (`--files: invalid JSON`), Paul is fixing it. Paul: *"just bump the translations numbers and continue... It doesn't exist yet, but it will be there by the time you release."* Auto CalVer in `release.yml` makes the next tag `2026.9.8`, so **2026.9.8 is hand-pinned on all four release branches** (09:40 UTC). Verify the tag exists before any production step; close the bot's pin PRs when they appear. |

## What is in it

- Monorepo `v1.10.6..main` (tip `9f9babd9fc`): the printer-setup redesign (roadmap#161 P0–P3, Specs G–M), sync fixes #1881 (money width) / #1886 (line-id retirement) / #1880 (401 hold), receipt-after-payment #1837, narrow-column #1888, logging noise #1878/#1887. Native-only: BLE/SPP lanes, native rasterising, per-profile URL schemes.
- Free `v1.10.7..main`: #1873 variation tombstone, #1865 JWT coexistence, #1881 digest upsert retry, search PRs #1834/#1852/#1856, perf #1841/#1842/#1846/#1853/#1854, #1832 receipt discount_type + filter.
- Pro `v1.10.7..main`: #511 plugin-details modal, #519/#520 perf companions.
- Electron `v1.10.7..main`: #404 printer logging, #409 Sentry-before-ready + damaged log rows, #411 USB product name + model query.

## Quick-check builds for Paul

- **Web:** https://wcpos.expo.app — production EAS deploy of tip `9f9babd9fc` (Deploy run 34023231538, deploy job green; served `entry-61eb793c…js` matches the job's export).
- **Electron dev:** worktree `~/Projects/monorepo-v2/.claude/worktrees/release-1.10.7` (branch `release/1.10.7`, app tip + `apps/electron` at `257e2dee`). Launch: `bash ~/.claude/scripts/dev-worktree.sh release-1.10.7 electron` (Expo on :8098).

## Sequence and gates

0. Translations tag lands → hand-pin on #1890, #412, #1889, #533.
1. Merge #1890 (Merge Gate ~30–60 min, restarts after the pin commit).
2. Wait for the merge SHA's main Deploy run (E2E must be green — the bundle workflow's `Require green main verification` gate).
3. `gh workflow run publish-web-bundle.yml --repo wcpos/monorepo --ref main -f monorepo_ref=<merge SHA> -f bundle_branch=main`.
4. Re-vendor `build/opfs.worker.js` from the new bundle commit into free #1889 (`assets/js/opfs.worker.js`) — the worker changed since v1.10.6 (`51f90b8f` → `370674fe` in `apps/main/public`). Mark #1889 ready, merge. Then merge pro #533.
5. Merge electron #412 after monorepo main is at 1.10.7; cancel the push-triggered Publish run; dispatch `tag-and-release.yml -f platform=all`; write the desktop release body by hand before the `release-notes` job runs.
6. **Held for Paul's go:** tag web-bundle `v1.10.17` + jsDelivr purge (production deploy to every 1.10 merchant), publish free draft (`gh release edit v1.10.8 --draft=false --latest` → WP.org deploy), pro `manual-release.yml` dispatch, electron draft publish, monorepo release `v1.10.7`.
7. Mobile: added by Paul mid-train (*"including ios and android to production"*) — run 34026553809.

## Notes / traps hit

- The pro clone's main working tree (`~/Projects/woocommerce-pos-pro`) has leftover **staged** edits from an earlier session (Edit_Store.php, Pro_Plugin_Updater.php, .pot, tests). `git pull` aborts there. Left untouched; all pro work done from a worktree off `origin/main`.
- usb native rebuild needs `CXXFLAGS="-std=c++17" pnpm rebuild:all` on the Xcode 26.5 SDK (node-addon-api 8.9 under usb). Monorepo memory: `electron-dev-worktree-install-recipe`.
- Monorepo `preinstall` only inits `apps/web`; `apps/electron` needs `git submodule update --init apps/electron` then a second `pnpm install --no-frozen-lockfile` before the electron app is in the workspace.

## Progress log (UTC)

- 09:46 translations `2026.9.8` released (Paul fixed the Aide task); pins already in place on all four branches.
- 09:52 free #1889: worker vendored from the release worktree's `apps/main/public/opfs.worker.js` (blob `370674fe`, byte-identical to what the bundle copies), marked ready. Drift check red by construction until v1.10.17 exists — not a required check.
- 09:55 Paul merged free #1889 himself (*"I can't wait"*) → main `d375953c`; `release.yml` run 34025983433 building the draft `v1.10.8`. **Paul publishes the free draft** (`gh release edit v1.10.8 --repo wcpos/woocommerce-pos --draft=false --latest`); that fires the WP.org deploy.
- 09:56 pro #533 merged (first `gh pr merge` refused with "base branch policy prohibits the merge" seconds after `gh pr ready`; state was CLEAN on retry) → main `ff10126f`; `manual-release.yml` dispatched for 1.10.8 (run 34026031204).
- monorepo #1890 Merge Gate still running (restarted 09:40 after the translations pin).
- 09:57 free draft `v1.10.8` body set to the changelog lines (release.yml leaves it empty); zip verified (1.10.8 ×3 markers, translations 2026.9.8, worker sha256 `6b2a0bec…`). **Paul published free v1.10.8** → `wporg-deploy.yml` run 34026103445.
- 09:59 pro `v1.10.8` published by manual-release (run 34026031204); zip verified: pro 1.10.8 ×3 markers, bundled free 1.10.8 + translations 2026.9.8 + worker `6b2a0bec…`, body = CHANGELOG entry.
- 10:00 **⚠️ WP.org deploy went to `tags/untagged-3d9b7ccddc54df87c672`.** Publishing the draft created the git tag `untagged-3d9b7ccddc54df87c672` (release `tag_name` became that too), so `wporg-deploy.yml` checked out that ref and the 10up action committed trunk + `tags/untagged-…` (SVN r3683263). Trunk's `Stable tag: 1.10.8` then pointed at a missing tag → WP.org warning to committers ("specified SVN tag /tags/1.10.8/ does not exist. /trunk/ will be used instead") and the API stayed on 1.10.7.
- 10:02 Fix: PATCH release 383535682 `tag_name=v1.10.8` (GitHub created the tag at `d375953c`), `make_latest=true`, deleted the `untagged-…` git ref, dispatched `wporg-deploy.yml --ref v1.10.8` (run 34026313378) to create SVN `tags/1.10.8`. **Leftover:** SVN `tags/untagged-3d9b7ccddc54df87c672/` still exists (needs SVN creds to delete; harmless but ugly).
- 10:03 monorepo #1890 Merge Gate pass (25m43s; state UNSTABLE = Native E2E lane, never blocking) → merged. Waiting on the merge SHA's main Deploy E2E before `Publish Web Bundle`.
- 10:04 `wporg-deploy.yml --ref v1.10.8` (run 34026313378) committed SVN `tags/1.10.8` (r3683265); WP.org API now reports 1.10.8 (10:04 GMT). **Free is live on WordPress.org.** Paul mid-fix: *"there is no fucking time left, make the wordpress.org plugin live now!!!"* — it was already live by then.
- 10:04 electron #412 merged → main `b7285884` (package.json 1.10.8); push-triggered `Publish` run 34026394759 cancelled; dispatched `tag-and-release.yml platform=all` → run 34026419616. Desktop draft body is written automatically from `scratchpad/desktop-release-body.md` as soon as the draft appears.
- 10:06 monorepo merge SHA `3387122ac3`. `Publish Web Bundle` dispatched (run 34026488578, `monorepo_ref=3387122a…`, `bundle_branch=main`, `override_release_gate` used: the merged tree is the PR head whose Merge Gate E2E passed at 10:03, so main's Deploy re-run cannot change the artifact; Paul: *"no time left"*). App release `v1.10.7` created on wcpos/monorepo (target `3387122a`, hand-written notes).
- 10:06 Paul: *"including ios and android to production"* → `build.yml platform=all profile=production submit=true` dispatched (run 34026508175). Note: the published plugin changelogs exclude native-only lines (BLE/SPP lanes, native rasterising) because mobile was not planned when they were written.
- 10:07 first mobile dispatch (34026508175) refused by `Require green main verification` (main Deploy for `3387122a` still running); re-dispatched with `override_release_gate` → run 34026553809 (~48 min, auto-submits to App Store Connect / Google Play).
- 10:10 `Publish Web Bundle` run 34026488578 green → web-bundle main `6636e979` ("build web bundle (@main from monorepo 3387122a…)"). Verified by SHA: entry `entry-0d9f487a…` (v1.10.16 was `entry-62a3c168…`), worker sha256 `6b2a0bec…` = plugin copy, bundle contains `"1.10.7"`. **Tagged `v1.10.17`**, purged jsDelivr (`@1.10` metadata + worker, `@main`), served `x-jsd-version: 1.10.17`. **Web app update is live for every 1.10 merchant.**
- 10:11 desktop draft `v1.10.8` appeared (id 383539521); body written by the monitor from `desktop-release-body.md` before the `release-notes` job could stamp the wrong app version.
- 10:14 **Root cause of the WP.org `untagged-…` deploy found:** PATCHing a *draft* release body without `tag_name` resets the tag to the `untagged-<hash>` placeholder (memory `github-draft-release-patch-resets-tag`). It hit electron too: the Forge Linux draft lost its tag after my body write, the Windows job made a second draft, both ended up `untagged-…`. Fix: PATCH 383539857 with `tag_name=v1.10.8` + body, re-uploaded the deb/rpm from 383539521 onto it, deleted 383539521. macOS jobs still running and will append to the canonical draft.
- 10:19 electron Publish run 34026419616 green (all four platforms); draft `v1.10.8` verified (9 assets = 1.10.7's set, body 1127 chars) and **published** at `b7285884`, marked latest.
- 10:20 electron Publish run 34026419616 green (all four platforms); draft `v1.10.8` verified (9 assets = 1.10.7's set, body 1127 chars); git tag `v1.10.8` created at `b7285884` (PATCH with a short `target_commitish` 422s) and **published**, marked latest.
- 10:19 bot translation-pin PRs closed as superseded (monorepo#1891, electron#413, free#1892, pro#534). Electron clone + free/pro release worktrees removed; the monorepo `release-1.10.7` worktree stays because the Electron dev build runs from it.
- 10:21 main Deploy run 34026368954 for `3387122a` completed green (all E2E shards + cold-start) — both `override_release_gate` uses (bundle, mobile) confirmed harmless after the fact.
- 10:28 free PHP Tests on main `d375953c` (run 34025983436, the one Paul flagged): all 8 matrix cells (PHP 7.4–8.5 × WP 7.0.4/7.1 × WC 11.0.1/11.1.0), PHPStan, Lint, Smoke green; only the Code Coverage measurement job still running.
- 10:36 Code Coverage job of that run failed on ONE assertion (`Test_Auth_Service::test_store_refresh_token_beyond_cap_bounds…`, stored expiry 1 s past the pre-captured `time()` — a clock race under xdebug; every non-coverage cell passed). Test-only flake; fixed in its own free PR (below). Not a release concern.
- 10:40 flake fix opened as its own PR: wcpos/woocommerce-pos#1895 (one-second tolerance; test-only).
- 10:57 mobile run 34026553809 green: iOS build `e3b848a1…` (buildNumber 19→20) and Android build `ea57e2c0…` (versionCode 21→22) built from `3387122a` and submitted ("All submissions have finished"); store review pending on Apple/Google.

## Final state

| Surface | Version | Where |
|---|---|---|
| WordPress.org | 1.10.8 | SVN `tags/1.10.8` r3683265, API 1.10.8 |
| Free GitHub | v1.10.8 | wcpos/woocommerce-pos, latest |
| Pro | v1.10.8 | wcpos/woocommerce-pos-pro, latest |
| Web bundle | v1.10.17 | jsDelivr `@1.10` → 1.10.17 |
| App release | v1.10.7 | wcpos/monorepo |
| Desktop | v1.10.8 | wcpos/electron, 9 assets, latest |
| iOS / Android | 1.10.7 (build 20 / versionCode 22) | submitted to App Store Connect / Google Play |

**Follow-ups:** delete SVN `tags/untagged-3d9b7ccddc54df87c672/` (needs SVN creds); merge wcpos/woocommerce-pos#1895 (test flake); consider a `release.yml` change so the free draft is created with its body so nobody PATCHes it by hand; consider making `wporg-deploy.yml` refuse an `untagged-` ref.
