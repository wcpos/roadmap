# WCPOS 1.10.4 release run — 2026-08-30

Patch release across the full train, cut the same day as 1.10.3. Mobile did not ship.

| Component | From | To | PR | Notes |
|---|---|---|---|---|
| App (`monorepo` `apps/main`) | 1.10.3 | 1.10.4 | wcpos/monorepo#1703 | Collided with #1702 — see below |
| Web bundle (`web-bundle`) | `v1.10.13` | `v1.10.14` | — | Blocked twice by a new release gate |
| Desktop (`electron`) | 1.10.4 | **1.10.5** | wcpos/electron#386 | Wrapper number stays one ahead of the app |
| Free plugin | 1.10.3 | 1.10.4 | wcpos/woocommerce-pos#1804 | |
| Pro plugin | 1.10.3 | 1.10.4 | wcpos/woocommerce-pos-pro#497 | **Zero Pro commits** — shipped only to carry free |

## The new thing this run: `Require green main verification`

`Publish Web Bundle` now runs a pre-flight gate (`.github/actions/require-green-main`, review on
monorepo #1687) **before** it builds anything. It resolves the push `Deploy` run on `main` for the
release SHA and requires that run's `🎭 E2E Tests` job to have concluded `success`. There is an
override — typing `I accept a red main` into `RELEASE_OVERRIDE` — which was not used and should not
be, because tagging a bundle version rolls every 1.10.x install on next load.

This gate did not exist on the 1.10.3 run, so **the run order in the 1.10.3 doc is now wrong**:
"merge the monorepo PR, then dispatch the bundle" fails, because the Deploy run the merge triggers
has barely started. The bundle dispatch has to wait ~15 minutes for E2E.

**It refused twice, for two different reasons, and neither was a broken test.**

1. **First dispatch, ~10 seconds after the merge:** `main verification still in_progress`. Purely
   my own impatience.
2. **Second attempt:** `E2E verification was superseded by a newer main push`. The gate reads a
   *cancelled* run as failure and deliberately refuses to fall back to an older green verdict — the
   check comment says a PR or preview run of the same SHA verifies a preview, not the shipped
   artifact. Any push to `main` mid-release therefore costs a full E2E cycle.

## Two agents cut the same release two minutes apart

wcpos/monorepo#1703 (mine, `apps/main/package.json` 1.10.3 → 1.10.4) merged at 05:47. Paul's
wcpos/monorepo#1702, also titled for the 1.10.4 release, merged at 05:49 and touched
`apps/main/app.config.ts` + a new `app-config-version.test.js`.

They turned out to be **complementary, not conflicting** — #1702 bumped no version number; it made
`app.config.ts` derive the version from `packageJson.version` and froze `DEV_CLIENT_NATIVE_VERSION`
for the dev-client profile so a release-only bump does not churn the EAS fingerprint. `package.json`
stayed the single source of truth. But that was luck, not coordination, and the second merge
cancelled the first's Deploy run, costing the E2E cycle above.

**Lesson: confirm who owns the monorepo bump before opening a PR for it.**

## `raw.githubusercontent.com` is cached, and it lied

After the bundle built, `raw.githubusercontent.com/.../main/build/metadata.json` returned the
**old** entry hash (`entry-1b4a3052…`), identical to `v1.10.13`. Taken at face value that reads as
"the build produced nothing new" and would have led either to a pointless rebuild or to tagging
while believing the bundle was unchanged.

The commit was fine. Fetching the same path through the **contents API pinned to the commit SHA**
returned the real entry, `entry-ea724e67…`, and the commit diff showed 10 changed files including
`build/opfs.worker.js` — the #1697 storage fix landing.

**Rule: verify bundle contents via `repos/.../contents/...?ref=<full-sha>`, or with the commit SHA
in the raw path. Never via `raw.githubusercontent.com/.../main/...`.**

## Bundle tags are tags, not releases

The 1.10.3 doc and the standing note both say to publish the bundle with
`gh release create vX.Y.Z --repo wcpos/web-bundle …`. That is **not** what the repo actually
contains: `v1.10.0` is the newest 1.10.x *release object*, while `v1.10.1` … `v1.10.13` are all
lightweight commit tags with no release. jsDelivr only needs the tag.

`v1.10.14` was created to match, via `POST repos/wcpos/web-bundle/git/refs`.

## Changelog: six lines, and what was cut

Built from a diff against `v1.10.3`, not from the milestone or PR titles. Six lines shipped.
Excluded, with the provenance test that caught each:

- **iOS drawer reopening** (monorepo #1698) — gated `Platform.OS !== 'web' && screenSize !== 'lg'`,
  so native phone only. Mobile did not ship this round and there is no `expo-updates`, so it reaches
  nobody. *Check the platform gate before writing a bullet, not the PR title.*
- **Session eviction signing out live devices** (free #1803) — the PR body opens *"Regression from
  #1798, which I shipped."* Introduced and fixed inside this cycle; no merchant on 1.10.3 saw it.
  Its sibling #1798 (login fatals on a multi-megabyte session row) **did** ship in 1.10.3 and is a
  bullet.
- `.pot` updates, e2e/CI hardening, FlashList patch-hash repointing — invisible.

Free #1799 (v2 catalog proxy rejected the sort with a 400) and #1802 (the `meta_key` INNER JOIN
dropped products missing the field) were merged into **one** bullet: two mechanisms, one merchant
story.

## Run order that actually worked

1. Bump + changelog PRs on all four repos, opened together.
2. Merge free → merge Pro (Pro's `composer.json` resolves free's `main` tip at build time; no SHA pin).
3. Publish free's draft by hand → fires `wporg-deploy.yml`.
4. Dispatch Pro `manual-release.yml -f releaseVersion=1.10.4`.
5. Merge the monorepo bump → **wait for the push Deploy run's E2E to go green** → dispatch
   `Publish Web Bundle` → verify the built entry → tag → purge jsDelivr.
6. Merge electron → cancel the push-triggered `Publish` run → dispatch `tag-and-release.yml
   -f platform=all` → **write the release body while the draft is still building** → publish.
7. Create the monorepo release last, so its notes can name the real bundle tag.

## Traps that recurred exactly as documented

- **A push to electron's `main` does not publish.** The push run built the app and skipped all four
  `publish-*` jobs. Cancelled, then dispatched.
- **Electron's `release-notes` job mirrors the monorepo release with the same tag.** With desktop at
  1.10.5 and the app at 1.10.4 it would have written "Desktop build of WCPOS app 1.10.5". Pre-empted
  by writing the body by hand as soon as the first platform publish created the draft — the job
  skips a non-empty body, and it did.
- **Pro's `manual-release.yml` extracts the top `##` section of `CHANGELOG.md`** into the release
  body. Verified: the body matches the changelog entry exactly, no manual overwrite needed.
- **Pro's plain-text `CHANGELOG`** (no extension) ships inside the zip and was updated alongside
  `CHANGELOG.md`.

## The storage fix did not reach web — the worker is vendored in the plugin

Caught by Paul **after** the release had shipped, and it invalidates one of the six changelog lines.

`Templates/Frontend.php` builds `opfsWorker` from `PLUGIN_URL . 'assets/js/opfs.worker.js'`. **The
web app loads the OPFS worker from the PHP plugin, not from the CDN bundle** — the bundle's own
`build/opfs.worker.js` is never fetched on web. Electron packages its own worker, so desktop is
unaffected. That is precisely why the divergence is invisible from both ends.

The plugin's copy is vendored by a hand-copy out of `wcpos/web-bundle`, with no script, no check and
no version marker. It was byte-identical to the bundle for **v1.10.0 → v1.10.12**, then diverged at
**v1.10.13** (the 1.10.3 release) and stayed diverged through v1.10.14.

| Copy | sha256 | Has monorepo #1697 |
|---|---|---|
| `assets/js/opfs.worker.js` as shipped in 1.10.4 | `c81e443c…` | ❌ |
| bundle `v1.10.13` | `1415cb56…` | ❌ |
| bundle `v1.10.14` | `141149890…` | ✅ |

Identifying strings from #1697 (`stale-changelog-after-compaction`, `wcpos-changelog-baked.txt`,
`hole:index-`, `boot-failed:`) are present in the bundle's worker and absent from the vendored one.

**So "The till repairs a damaged local database at startup" is false for web merchants in 1.10.4** —
true for desktop 1.10.5 only. The claim is live on WordPress.org, in both plugin release bodies and
inside the Pro zip. Paul's call: **vendor to `main` and let it ride the next release**, rather than
cutting a 1.10.5 or amending the live notes.

Fixed forward in wcpos/woocommerce-pos#1806, which re-vendors the worker and adds
`opfs-worker-drift.yml` — resolves the plugin's `major.minor` to the highest matching bundle tag and
compares sha256. It runs **daily** as well as on PRs, because the drift is caused by a tag in another
repo that fires nothing in the plugin repo. Verified both ways: passes on the re-vendored worker,
and reproduces the 1.10.4 drift on the old one.

**The wider lesson: "the bundle shipped it" is not the same as "merchants have it."** Any monorepo
fix that lands in a worker rather than the entry bundle needs a matching plugin change, and until
PR #1806, nothing said so.

**A retracted claim from this same investigation, worth recording as a method error.** I reported a
6.8 MB `assets/js/entry-62e520b6….js` and a stale `chart-<hash>.js` as "still shipping in the free
zip", from an `ls` of the working clone. They do not ship and are not in the repo: `.gitignore` line
22 is `assets/js/*` with a four-entry allowlist (`README.md`, `indexeddb.worker.js`,
`opfs.worker.js`, `api-fetch-method-param.js`), and everything else under `assets/js/` is compiled by
CI at build time. `git ls-files assets/js/` returns exactly those four, and `unzip -l` on the shipped
1.10.4 artifact lists eight files, none of them the leftovers.

**`ls` of a working clone is not evidence about a build artifact.** Use `git ls-files` for what is in
the repo and `unzip -l` on the release asset for what ships. This is the same class of error as
trusting `raw.githubusercontent.com` earlier in the run: reading a convenient surface instead of the
authoritative one.

## Outcome

All five artifacts shipped 2026-08-30.

| Component | Version | Verified by |
|---|---|---|
| Free plugin | 1.10.4 | zip header + `\VERSION` + `Stable tag` + changelog all 1.10.4; draft targeted the real merge commit `a7165f45`; SVN trunk `Stable tag: 1.10.4`, `tags/1.10.4/` HTTP 200; wp.org API 1.10.4 |
| Pro plugin | 1.10.4 | both Pro markers 1.10.4; **`vendor/wcpos/woocommerce-pos` reads 1.10.4** (proof free merged first); plain `CHANGELOG` present in the zip |
| App (`monorepo`) | 1.10.4 | release `v1.10.4` at `a4fe2e81` |
| Desktop (`electron`) | **1.10.5** | 9 assets across macOS x64/arm64, Windows, Linux; body reads app **1.10.4** |
| Web bundle | `v1.10.14` @ `63d7805c` | jsDelivr `x-jsd-version: 1.10.14`; served entry is **byte-identical** (SHA-256) to the tagged blob and embeds `"1.10.4"` |

### Follow-ups

- **Re-vendoring `assets/js/opfs.worker.js` is now a release step** whenever a new bundle tag ships.
  Guarded by free #1806, but the guard signals rather than blocks.
- **Update the standing notes**: the bundle ships as a *tag*, not a release; and the run order must
  now include the E2E wait before the bundle dispatch.
- Fix electron's `release-notes` job to read the bundled app version instead of its own tag. Third
  run in a row this has needed a manual workaround.
- Decide whether to backfill or delete electron's `CHANGELOG.md` (unmaintained since 1.9.5).
- Clean up or `.distignore` free's stale `CHANGELOG.md` — still shipping in the zip at 20 KB, still
  titled "WCPOS 1.8.15 — Changelog Draft".
- The monorepo's `apps/web` pin is at bundle `v1.10.9` while the fleet is on `v1.10.14`. Confirmed
  reference-only, but the drift is now five tags wide and worth either automating or deleting.
