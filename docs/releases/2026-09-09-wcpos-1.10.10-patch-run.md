# 2026-09-09 — WCPOS 1.10.10 patch train (run record)

Status: **complete** (18:34 UTC) — plugins, bundle and desktop live; WP.org deployed via the fixed workflow; iOS/Android submitted and pending store review. Paul: *"I want to release a patch release. It's got some updates. Can you start preparing that? For PHP and Electron and all apps (web app, phone app as well)"* — mobile production build + submit is in scope for this train.

## Versions

| Line | Was | Now |
|---|---|---|
| Free plugin | 1.10.9 | 1.10.10 |
| Pro plugin | 1.10.9 | 1.10.10 |
| App (`apps/main`) | 1.10.8 | 1.10.9 |
| Web bundle | v1.10.18 | v1.10.19 (from the release merge SHA) |
| Desktop | 1.10.9 | 1.10.10 (no electron-side changes; ships app 1.10.9) |
| Translations | 2026.9.9 | 2026.9.9 (unchanged; already pinned on all four mains) |

## What is in it

- Free `v1.10.9..main` (tip `17db8841`): wcpos/woocommerce-pos#1918 cashiers can no longer edit staff accounts (privilege escalation, reported by a Pro user); wcpos/woocommerce-pos#1917 multi-role users (Members plugin) no longer 403 at the POS gates, error names the missing capability; wcpos/woocommerce-pos#1919 WooCommerce Tax rate identities preserved on POS saves; wcpos/woocommerce-pos#1907 + #1913 store street sent to TaxJar for base-taxed sales; wcpos/woocommerce-pos#1906 SVN tag-delete workflow hardening (CI); #1728/#1914 tests; #1920–#1922 i18n + translations 2026.9.9.
- Pro `v1.10.9..main` (tip `204ef1a`): wcpos/woocommerce-pos-pro#543 translations 2026.9.9 only.
- Monorepo `v1.10.8..main` (tip `2e1b32260b`): wcpos/monorepo#1928 + #1923 native cart quantity (select on focus, two-digit clip); wcpos/monorepo#1920 variation prefetch presence in one query (iPad idle stalls 9.5 s → 0.5 s per tick); wcpos/monorepo#1921 winspool queue hidden behind the printers empty state + generic BLE/SPP validation on native; wcpos/monorepo#1918 expo-opfs native copy/move swap + orphaned-swap recovery; wcpos/monorepo#1910 derivable-ledger rebuild on multi-instance recovery refusal (web SYNC999 loop); wcpos/monorepo#1909 push-rejection fingerprints; wcpos/monorepo#1906 + #1914 native Sentry crash reporting behind telemetry consent + symbol upload; #1907/#1919 tests; #1925 translations.
- Electron `v1.10.9..main`: wcpos/electron#416 translations 2026.9.9 only.
- New deps since app 1.10.8 (`@sentry/react-native`, `expo-application`, `expo-file-system`): none has an install script, so no `onlyBuiltDependencies` gap for the strict bundle install.
- OPFS worker: `packages/database` has only test changes since v1.10.8, so the worker is expected unchanged — verify by hash against v1.10.18 after the bundle builds before deciding on a re-vendor.

## Pre-flight

- No stale drafts in free/pro/electron release lists (all `v1.10.9` published).
- Main Deploy for monorepo tip `2e1b32260b` (run 34379920577) was still in progress at 17:xx UTC; the previous three main Deploys are green.
- Pro's local `main` working tree has **uncommitted staged changes** (Edit_Store.php `_method` shim dependency, Pro_Plugin_Updater.php, pot, tests, a version line) that are not mine — `git pull` refused. Worked from a worktree off `origin/main` instead; the main tree was left untouched.
- Leftover clone `~/Projects/monorepo-v2-worktrees/electron-release-1.10.5` from an earlier train is still on disk.

## PRs

| Repo | PR | Branch | State at open |
|---|---|---|---|
| wcpos/woocommerce-pos | #1924 | `release/1.10.10` | ready |
| wcpos/woocommerce-pos-pro | #546 | `release/1.10.10` | draft (merge after free) |
| wcpos/monorepo | #1930 | `release/1.10.9` | ready |
| wcpos/electron | #417 | `release/1.10.10` | draft (merge after monorepo main is at 1.10.9) |

## Sequence

1. Merge monorepo #1930 → Publish Web Bundle from the merge SHA (override the main-verification gate only if main's Deploy for that SHA is still running and the commit is a pure bump) → verify by SHA → **tag `v1.10.19` (held)** → purge jsDelivr.
2. Merge free #1924 → set the draft body **with `tag_name`** → **Paul publishes** → verify SVN `tags/1.10.10`.
3. Merge pro #546 → **`manual-release.yml` dispatch (held)** → verify the zip bundles free 1.10.10.
4. Merge electron #417 after monorepo main is at 1.10.9 → cancel push Publish → dispatch `platform=all` (draft) → set the draft body **with `tag_name`** → **publish (held)** with the git tag created at the full SHA.
5. **Mobile production build + submit (held)**: `build.yml platform=all profile=production submit=true` from main once at 1.10.9.

## Progress log (UTC)
- 17:26 wcpos/woocommerce-pos#1924 Merge Gate pass → merged (`655c54e6`). release.yml building draft v1.10.10. Pro #546 gate passed earlier; marked ready.
- 17:3x pro #546 direct merge refused ("base branch policy prohibits the merge" — same transient as 09-06/09-07 while the non-required Greptile review runs); auto-merge enabled instead.
- 17:28 pro #546 merged via auto-merge (`97b97ea9`). manual-release dispatch HELD for Paul's go.
- 17:29 free release.yml run 34382996767 green → draft v1.10.10 (id 385717001, target `655c54e6`), body set WITH tag_name (tag survived). Zip verified: 1.10.10 ×3 markers, translations 2026.9.9, worker `6b2a0bec…` (= bundle v1.10.18). HELD for Paul's publish.
- 17:31 **Paul published free v1.10.10** — tag `v1.10.10` at `655c54e6`, wporg-deploy firing. Go received: carrying pro/bundle/desktop/mobile through.
- 17:3x pro manual-release.yml dispatched for 1.10.10 (free main tip = 655c54e6, already merged).
- 17:33 ⚠️ wporg-deploy run 34383446938 reported **success but deployed nothing**: the 📦 WordPress Plugin Deploy step died in the action's own `apt-get update` (dl.google.com chrome repo `Hash Sum mismatch`, exit 100) and the step is continue-on-error, so SVN still said 1.10.9 / tags/1.10.10 404. Rerun dispatched. Follow-up: the deploy step must fail closed (see fail-closed-cleanup-jobs memory).
- 17:35 pro manual-release run 34383579508 green → **pro v1.10.10 published** (latest, woocommerce-pos-pro.zip).
- 17:37 pro zip verified: 1.10.10 ×3 markers, translations 2026.9.9, vendored free 1.10.10, worker `6b2a0bec…`; release body = CHANGELOG entry.
- 17:36 wporg-deploy attempt 2 failed identically (apt `Hash Sum mismatch` on the runner's Google Chrome source inside the 10up action's `apt-get update`). Attempt 3 re-run 17:4x; fix branch `ci/wporg-deploy-apt` drafted (drop vendor apt sources, remove continue-on-error, dispatch-by-version).
- 17:40 wcpos/monorepo#1930 Merge Gate pass → merged (`79b2dade`). Publish Web Bundle dispatched (monorepo_ref = merge SHA, bundle_branch = main, gate override: pure bump + submodule pointers on a tip whose Deploy was green). Mobile build.yml platform=all profile=production submit=true dispatched from main (same override). Electron #417 marked ready.
- 17:42 wporg-deploy attempt 3 failed identically — not a flake. Fix PR wcpos/woocommerce-pos#1925 opened (drop vendor apt sources, fail closed, dispatch-by-version with tag checkout); WP.org deploy will go via dispatch `version=1.10.10` once merged. WP.org is the only leg still on 1.10.9.
- 17:43 electron #417 merged (`afda3e69`); push-triggered Publish 34384621087 cancelled; tag-and-release platform=all dispatched.
- 17:45 app release `v1.10.9` created on wcpos/monorepo at `79b2dade` (record; notes = train notes). Electron Publish run 34384652818 building; desktop body prepared for hand-write once the draft appears.
- 17:47 #1925 Merge Gate pass → merged (`6a3e0db3`). wporg-deploy dispatched with version=1.10.10 (checks out tag v1.10.10, VERSION passed explicitly).
- 17:45 Publish Web Bundle run 34384523311 green → web-bundle main `4c2dae3f` from monorepo `79b2dade`; verified by SHA (entry `entry-27820bf3…` vs v1.10.18 `entry-8eb4bec0…`; worker unchanged `6b2a0bec…` → no re-vendor; app 1.10.9 present).
- 17:47 web-bundle **tagged `v1.10.19`** at `4c2dae3f`; jsDelivr purged (took ~1 min for @1.10 to re-resolve); `@1.10` serves `x-jsd-version: 1.10.19`, entry `entry-27820bf3…`. Web app update live.
- 17:47 wporg-deploy dispatch run 34384880559 (fixed workflow, version=1.10.10) green: `Committed revision 3688785`; SVN trunk `Stable tag: 1.10.10`, `tags/1.10.10` HTTP 200. **WP.org live.** Net delay vs publish: ~16 min.
- 17:50 electron Publish run 34384652818: ONE draft (id 385729829, tag v1.10.10), body set WITH tag_name before release-notes ran. **publish-linux failed** at "Install Linux build dependencies" — `sudo apt-get update` on the same broken runner apt source as wporg-deploy. macOS ×2 and Windows still running. Fix branch `ci/linux-apt-sources` in progress; plan: merge, re-dispatch `platform=linux` onto the same draft.
- 17:55 electron fix PR wcpos/electron#418 opened (drop vendor apt sources before apt-get update in publish-linux). Job log not downloadable while the run is in progress; cause inferred from step name + identical timing/signature to wporg-deploy.
- 17:57 electron #418 merged (`7d983246`); push Publish cancelled; tag-and-release `platform=linux` dispatched to append Linux assets to draft 385729829.
- 18:03 Linux-only run 34385855004 green (fix worked first time): .deb + .rpm appended to draft 385729829 → 7 assets. publish-macos (Intel) from the original run still going.
- 18:05 original electron run 34384652818 finished (publish-linux failure from before the fix is its only red job); macOS x64 assets landed → draft 385729829 has 9 assets (same set as 1.10.9).
- 18:05 electron git tag `v1.10.10` at `7d983246` (main tip: bump + apt fix); draft 385729829 **published**, latest, 9 assets, body intact. Desktop live.
- 18:08 WP.org API reports 1.10.10 and the 1.10.10 download zip is HTTP 200 (21 min after the SVN commit).
