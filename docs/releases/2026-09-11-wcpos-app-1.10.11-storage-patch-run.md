# 2026-09-11 — WCPOS app 1.10.11 patch train (storage compaction, Cloudflare HOST121) — run record

Status: **complete** for the authorised scope (web bundle, app release, desktop live). Plugin leg NOT run — see correction below. Paul: *"I would like to release a patch release which has some electron fixes"* → after the survey, *"do as you recommend"* = app, web bundle and desktop only; **no plugin leg, no mobile leg** in the authorised scope.

**Correction found after the go (not yet decided by Paul):** web loads `opfs.worker.js` from the free plugin, not the bundle (see memory `opfs-worker-vendored-in-plugin`), and this train changes that worker (#1955 rebuilt it; #1952 patches the rxdb-premium code inside it). So the storage fixes reach **desktop** via 1.10.14 and reach **web** only when free re-vendors the worker from bundle v1.10.21 — which means a free 1.10.11 (and Pro 1.10.11, since Pro vendors free). The free `opfs-worker-drift` daily check will go red after the v1.10.21 tag. Raised with Paul at the end of the run.

## Versions

| Line | Was | Now |
|---|---|---|
| Free plugin | 1.10.10 | 1.10.10 (unchanged in scope; see correction) |
| Pro plugin | 1.10.10 | 1.10.10 (unchanged) |
| App (`apps/main`) | 1.10.10 | 1.10.11 |
| Web bundle | v1.10.20 | v1.10.21 (from the release merge SHA) |
| Desktop | 1.10.13 | 1.10.14 (`.monorepo-ref` v1.10.10 → v1.10.11; published 15:5x UTC) |
| Translations | 2026.9.9 | 2026.9.9 (unchanged on all four lines; bot PR wcpos/translations#309 left open) |

## What is in it

- Monorepo `v1.10.10..main` (tip `11dc0699`): #1952 + #1955 storage (compaction over large gaps without a gap-sized string, windowed index rebuild, NUL-filled ranges dropped as hollow rows); #1944 Cloudflare challenge during discovery → HOST121; #1940 browser AbortError never renamed (Firefox/Safari); #1951 create push strips line ids, handler CanceledError logs at debug. Rest are `docs(context)` commits. No `pnpm-lock.yaml` change → no `onlyBuiltDependencies` gap. `apps/main/public/opfs.worker.js` and `packages/database/src/plugins/opfs-targeted-recovery.mjs` changed → the bundle's worker WILL differ from v1.10.20.
- Electron `v1.10.13..main` (tip `433fe73`): #429 storage mirror + telemetry dedupe by event class; #430/#432 `next` prerelease lane (CI only). No lock change. `Monorepo Drift` on main is red because #429's copies are ahead of the v1.10.10 pin; #434 fixes that by pinning v1.10.11. #433 (SHA pin) closed as superseded.

## Release notes (app release body; electron's `release-notes` job copies it onto the desktop draft)

- **The till's local database can shrink again after it has grown large.** The clean-up step that reclaims space failed on every attempt once the gap it had to close passed a few hundred megabytes, so the database file only ever grew (one desktop install reached 42 GB), and the start-up index rebuild then failed on the same file. Both now work in bounded windows and the file returns to its real size.
- **Windows: rows zeroed by a power cut or an unfinished write no longer stop database clean-up.** They were read as foreign data and refused, so clean-up failed on that collection on every pass. They are now recognised as empty and dropped.
- **Stores behind Cloudflare: a security check during connection now reports HOST121 with a link to the Cloudflare guide**, instead of "Site does not seem to be a WordPress site".
- **Firefox and Safari: cancelling a product search no longer records a SYNC321 error.**
- **A new order sent to WooCommerce no longer carries stale line ids.** WooCommerce rejected the order with "invalid item id" when the same order had already been acknowledged once.

Desktop-only line (hand-written on the electron draft, above the app notes):
- **Desktop: a damaged local database no longer sends thousands of identical error reports.** One install's repair pass produced almost 11,000 reports in an hour; reports are now grouped by cause.

Struck: CanceledError debug logging (telemetry), `next` Publish lane (CI), docs/context commits.

## Pre-flight

- Monorepo main tip `11dc0699` Deploy green (run 34574479327) → the bundle gate will pass on the release merge SHA once its own Deploy finishes; override rule stands (pure bump on a green tip).
- No stale drafts: monorepo latest v1.10.10, electron latest v1.10.13 (plus `-next.61x` prereleases from the new lane), web-bundle tip `b30b001d` = v1.10.20. Free plugin `assets/js/opfs.worker.js` sha `370674fe` = bundle v1.10.20 worker (in step today).
- Worktrees: `~/Projects/monorepo-v2-worktrees/release-1.10.11` (from origin/main, `apps/web` gitlink moved with `update-index --cacheinfo`, no submodule checkout), `~/Projects/electron-worktrees/release-1.10.14`.

## PRs

| Repo | PR | Branch | State at open |
|---|---|---|---|
| wcpos/monorepo | #1978 | `release/1.10.11` | ready (`96ebe095`; `apps/main` 1.10.11, `apps/web` → `b30b001d` = v1.10.20 reference pin) |
| wcpos/electron | #434 | `release/1.10.14` | draft (`cac8b41`; `drift` red until the v1.10.11 tag exists) |

## Sequence

1. Merge #1978 → Publish Web Bundle from the merge SHA → verify by SHA (entry hash new; `opfs.worker.js` hash vs v1.10.20 expected to CHANGE; app 1.10.11 present) → tag `v1.10.21` → purge jsDelivr → confirm `@1.10` serves 1.10.21.
2. App release `v1.10.11` on wcpos/monorepo at the full merge SHA with the notes above; `--latest`.
3. Mark #434 ready → merge → cancel the push-triggered Publish → dispatch `Publish` `platform=all` → draft → set body (desktop line + app notes, WITH `tag_name`) → git tag `v1.10.14` at the full merge SHA → publish, latest.
4. Not in scope: mobile build/submit; plugin release (see correction above).

## Progress log (UTC)
- 14:52 wcpos/monorepo#1978 opened (`96ebe095`), Merge Gate running. wcpos/electron#434 opened as draft (`cac8b41`); #433 closed as superseded.
- 14:58 Merge Gate red: Lint's script tests failed once on `packages/virtual-printer/self-signed-cert.test.mjs` (asn1 illegal padding — nondeterministic; identical code green on main run 34574479369). Re-ran failed jobs (run 34612574388).

## Follow-ups

- **Flaky test, not this train's:** `packages/virtual-printer/self-signed-cert.mjs` line 50 builds the serial from `crypto.randomBytes(8)`; `der.int` pads a high-bit first byte but never strips a leading `0x00`, so a serial starting `00 <byte < 0x80>` is DER-illegal padding (~1 in 512 runs) and `X509Certificate` throws `asn1 encoding routines::illegal padding`. Fix: drop leading zero bytes (or force `buf[0] |= 0x01` is wrong — strip, then pad). Worth a one-line PR on `main`.
- 15:04 Paul: *"wait a moment, another agent is proposing a PR"* — train HELD before the monorepo merge; nothing merged, tagged or released. Expecting to fold that PR into 1.10.11 (rebase release/1.10.11 onto it) once it lands.
- 15:11 Paul: *"ok, continue"* — no new commit landed on main in any repo (the other agent's PRs were `next`-lane: monorepo#1977, free#1955). Paul asked to verify the electron pin is at/after the worker rebuild: `.monorepo-ref` v1.10.11 = #1978 merge SHA, descends from 11dc0699 (#1955 rebuild, 07:37 UTC); worker blob f3785c11 identical on the release branch, carries `hollow-row-dropped`. Desktop covered; web still needs the free plugin re-vendor.
- 15:12 Merge Gate green on re-run (Lint flake cleared) → wcpos/monorepo#1978 merged (`24387b91`). Publish Web Bundle dispatched (monorepo_ref = 24387b91, bundle_branch = main, gate override: pure version bump on the green tip 11dc0699 — the merge SHA's own Deploy had just started and cannot change the artifact). App release `v1.10.11` created at 24387b91 (latest; notes = the five lines above).
- 15:15 wcpos/electron#434 drift green after the v1.10.11 tag → marked ready → merged (`9ba4021c`). Push-triggered Publish 34614922607 cancelled; Publish platform=all dispatched.
- 15:17 Publish Web Bundle run 34614779949 green → web-bundle main `b181e8c7` from monorepo 24387b91; verified by SHA (entry `entry-a9c57d86…` vs v1.10.20 `entry-06963f47…`; `opfs.worker.js` blob f3785c11 = the monorepo rebuild, carries `hollow-row-dropped`; app 1.10.11 in the entry). Tagged `v1.10.21` at b181e8c7; jsDelivr purged; `@1.10` serves x-jsd-version 1.10.21. **Web app update live** (worker fix reaches web only via the plugin re-vendor — free drift check will now go red).
- 15:31 electron Publish run 34614975932 green (all 7 jobs); draft v1.10.14 had 9 assets and the app notes already copied by `release-notes`. Desktop-only telemetry line prepended (PATCH with tag_name); tag `v1.10.14` at 9ba4021c; draft **published**, latest. **Desktop live** (app 1.10.11 + compaction/recovery + telemetry dedupe). Train complete for the authorised scope.
