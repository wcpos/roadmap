# 1.10.20 patch train — run record

Started 2026-09-16 ~12:55 UTC on Paul's request ("I want to do a patch release containing todays fixes across all platforms: web, desktop, and native OTA"). Seven merges landed on monorepo `main` after the 1.10.19 release SHA `00b3763a`, four of them merchant-facing, and one of them (#2102) rebuilt `apps/main/public/opfs.worker.js` — so, as on the 1.10.17 train, the plugin legs are required and the tag order inverts (bundle → vendor → tag → free). The free plugin also carried four merges of its own since v1.10.15. Pro has no commits of its own and follows because it vendors free.

## Version lines

| Line | Released | This train | PR |
|---|---|---|---|
| Monorepo app | v1.10.19 | **v1.10.20** | [monorepo#2108](https://github.com/wcpos/monorepo/pull/2108) |
| Web bundle | v1.10.29 | **v1.10.30** | tag only |
| Electron desktop | 1.10.22 | **1.10.23** | [electron#458](https://github.com/wcpos/electron/pull/458) |
| Free plugin | 1.10.15 | **1.10.16** | [woocommerce-pos#2000](https://github.com/wcpos/woocommerce-pos/pull/2000) |
| Pro plugin | 1.10.15 | **1.10.16** | [woocommerce-pos-pro#573](https://github.com/wcpos/woocommerce-pos-pro/pull/573) |
| iOS / Android | 1.10.18 store build + 1.10.19 OTA | **OTA update on `production`** | `publish-mobile-update.yml` at the release SHA |
| Translations pin | 2026.9.10 | unchanged | latest tag is 2026.9.10 |

## What rides

Monorepo `v1.10.19...main` (tip `53a10283` when cut; release commit `ed90be30`):

- [#2101](https://github.com/wcpos/monorepo/pull/2101) — census probes no longer starve under a sticky `X-WCPOS-Pressure: high` header: `readRunnable` orders by attempt/age not key, a starvation tick probes all nine census collections, and a "high" header on a fast response no longer trips back-off. Client half of the thechristadelphian.com "checking…" case; the server half is free #1998.
- [#2102](https://github.com/wcpos/monorepo/pull/2102) — rxdb-premium cleanup compaction batched (1000 docs / 250 ms per call, one index bake per batch, one broadcast) via a new postinstall patch; **rebuilds `opfs.worker.js`** (blob `4b78bf97` → `3824bc10`). Idle web POS at 30% CPU for minutes. Web merchants get this through free 1.10.16 (the plugin serves the worker); desktop through electron#457's byte-identical copy of the patch.
- [#2104](https://github.com/wcpos/monorepo/pull/2104) — Health > Performance says what pace the till keeps with the server and why (Normal / Easing off ×N: reason / Paused until).
- [#2105](https://github.com/wcpos/monorepo/pull/2105) — variation popover Add to Cart honours `prevent_overselling` like the cart guard does (two stores reported today).
- CI/test only, ship in no artifact: [#2096](https://github.com/wcpos/monorepo/pull/2096) (Android runtime pre-resolve in `e2e-native.yml`), [#2099](https://github.com/wcpos/monorepo/pull/2099) (Maestro README), [#2106](https://github.com/wcpos/monorepo/pull/2106) (search-heap trough gate).

Free `v1.10.15...main` (tip `feaa6a2d`):

- [#1995](https://github.com/wcpos/woocommerce-pos/pull/1995) — tombstone seed v2: every install re-announces every hidden product/variation on its first request after upgrade, so tills drop stale Online Only copies on the next ordinary pull.
- [#1997](https://github.com/wcpos/woocommerce-pos/pull/1997) — POS Only / Online Only admin counts restricted to WordPress's "All" statuses.
- [#1998](https://github.com/wcpos/woocommerce-pos/pull/1998) — Ping no longer reports pressure off a guessed 1-CPU divisor when `/proc/cpuinfo` is unreadable.
- [#1999](https://github.com/wcpos/woocommerce-pos/pull/1999) — Ping fast path sends `Cache-Control: private, no-store` (+ LiteSpeed no-cache) like every other POS response.

Pro `v1.10.15...main`: no commits. Electron `v1.10.22...main`: [#457](https://github.com/wcpos/electron/pull/457) (mirror of #2102's patch; moved `.monorepo-ref` to the #2102 branch SHA `1c97e504`, restored to a tag by the release PR).

## Release notes (app v1.10.20)

- **Cashiers can sell an out-of-stock variation again when "Avoid overselling" is off.** The variation picker's Add to Cart button disabled on stock alone and ignored the store setting, so an out-of-stock simple product could be sold but an out-of-stock variation could not. The button now follows the same rule as the cart: it disables only while the setting is on and the variation is unsellable.
- **An idle web or desktop POS no longer sits at 30% CPU after the cashier stops.** Local database cleanup compacted storage fifty records at a time and rewrote every index between batches, so a till with a large backlog of deleted records churned for minutes. Cleanup now compacts in one batch and rewrites the indexes once. Web merchants receive this part through WooCommerce POS plugin 1.10.16, which serves the storage worker; desktop and phone apps carry it in this release.
- **Health > Database no longer shows "checking…" forever on a server that reports high load.** A host that stamps every response as under high load while answering in a tenth of a second kept the till backed off, and the backed-off checks always started with the same three record types, so the other six never got a turn. Checks now rotate fairly, all record types are probed on each backed-off tick, and a "high load" header on a fast reply no longer counts against the server.
- **Health > Performance says what pace the till is keeping with the server, and why.** One line at the top of "Your server, over time" reads Normal pace, Easing off (with the factor and the reason), or Paused until a given time.

Phone and tablet apps on the 1.10.18 store build receive this version as an over-the-air update: it downloads in the background and applies on the next cold start after that, with no store update needed. Older store versions must first update to 1.10.18 from the App Store or Google Play.

## Release notes (plugins 1.10.16, free readme + Pro CHANGELOG)

- **Tills stop showing products the store has hidden from the POS.** Between 1.10.1 and 1.10.14 the POS product search could return a hidden product (fixed in 1.10.15), and a till that searched during that window kept a copy. Upgrading now writes a fresh removal notice for every hidden product and variation, and every till drops its stale copies on its next ordinary sync, with no reset or manual sync needed.
- **The POS Only and Online Only counts above the WooCommerce products list are accurate.** They counted trashed and auto-draft products that the view never lists; they now count the same statuses as WordPress's own "All" view.
- **The server no longer reports high load from a guessed CPU count.** On hosts that hide `/proc/cpuinfo`, the load average was divided by one CPU, so any load above 1.8 read as "high" forever and every till slowed its sync. When the CPU count is unknown, no load is reported.
- **The connection check can no longer be served from a page cache.** The public ping was answered before the cache-control headers were set, so a cached "ok" could mask an outage for the cache's lifetime. It now sends the same no-store headers as every other POS response.
- **An idle web POS no longer sits at 30% CPU after the cashier stops.** Local database cleanup compacted storage fifty records at a time and rewrote every index between batches; it now compacts in one batch and rewrites the indexes once. The web app loads its storage worker from this plugin, so web merchants receive this fix through this update rather than through the app bundle.

## OTA decision

`eas fingerprint:compare --build-id` from `apps/main` (`EAS_BUILD_PROFILE=production`, this session's worktree detached at the release commit `ed90be30`, `pnpm install --offline --frozen-lockfile`):

| Platform | Shipped 1.10.18 build | Runtime version | Result |
|---|---|---|---|
| Android | `247b12c8-4d98-47d2-8f3e-9840666d211d` | `050c0144c106f24ab23db6509c37f998f4c1dddd` | ✅ matches local |
| iOS | `d650ce31-d233-481f-99dc-edc037e58fab` | `612a275dcc877f7d400f203cd56c0b8f77490683` | ✅ matches local |

The diff since 1.10.19 touches `package.json` (root, postinstall script list), `apps/main/__tests__/ota-updates-config.test.ts`, `apps/main/public/opfs.worker.js`, `packages/*` TS and the e2e workflow — none of it a fingerprint source. The waiter dispatches the OTA from the merge SHA only if the merge commit's tree equals `ed90be30`'s tree (main did not move between cut and merge); otherwise the compare is re-run at the merge SHA first.

## Sequence

1. `release/1.10.20` cut at `53a10283` through the git-data API (`ed90be30`), #2108 opened ready with auto-merge queued.
2. Free `release/1.10.16` (`ad14cd68`, markers + readme entry), Pro `release/1.10.16` (`fcc0048c`), electron `release/1.10.23` (`b1b75cd4`, `.monorepo-ref` → `v1.10.20`) cut the same way; #2000 / #573 / #458 opened as drafts.
3. #2108 Merge Gate → merge → push Deploy on the merge SHA is the gate → `Publish Web Bundle` (`bundle_branch=main`) + `publish-mobile-update.yml` (`channel=production`) dispatched from it.
4. Bundle green → vendor `build/opfs.worker.js` onto free's release branch (blob-SHA verified) → tag web-bundle `v1.10.30` → read `@1.10`, purge only if stale → confirm `x-jsd-version: 1.10.30`.
5. `gh release create v1.10.20` at the merge SHA, `--latest`.
6. Free #2000: drift rerun against the real tag → ready → merge → `release.yml` builds the draft → publish → `wporg-deploy.yml` → verify SVN `Stable tag: 1.10.16`.
7. Electron #458: drift rerun once the app tag exists → ready → merge → cancel the push Publish → `tag-and-release.yml platform=all` → tag at the merge SHA → publish the draft.
8. Pro #573: merge → `manual-release.yml releaseVersion=1.10.16` → verify the zip vendors free 1.10.16 and worker `3824bc10`.
9. OTA run green → `eas update:list --branch production --json` shows both groups at the runtime versions above.
10. `main` → `next` sync PR.

## Log (UTC)

- 12:5x — scoped from tag diffs on all five lines; worker moved at #2102; peers idle (nobody else on a train).
- 13:04 — `release/1.10.20` cut (`ed90be30`); #2108 open, auto-merge queued.
- 13:1x — free `release/1.10.16` (`ad14cd68`), Pro `release/1.10.16` (`fcc0048c`), electron `release/1.10.23` (`b1b75cd4`) cut; #2000, #573, #458 open as drafts. Fingerprint compare at `ed90be30`: iOS `612a275d…` and Android `050c0144…` both match the 1.10.18 store builds.
- 13:18 — **Paul merged free #2000 before the bundle existed** (`e82dffc5`): free `main` carries the 1.10.16 markers with the OLD worker `4b78bf97`; `release.yml` built draft `v1.10.16` (no tag until publish). Recovery path is built into `release.yml`: a further push while the draft exists "reuses it" and re-uploads the zip. Draft held unpublished.
- 13:19 — #2108 Merge Gate green; merged at **`5bb4577d`**. Paul: don't wait on the push Deploy for a version bump — the bundle and OTA workflows' `require-green-main` gate was overridden (`override_release_gate='I accept a red main'`) and the remaining release PRs admin-merged. Merge tree `3e6089c9` == release-commit tree, so the `ed90be30` fingerprint compare stands for the merge SHA.
- 13:20 — `Publish Web Bundle` run 35101321433 and `publish-mobile-update.yml` run 35101355561 dispatched from `5bb4577d`. App release **v1.10.20** created at `5bb4577d`, `--latest`.
- 13:21 — electron #458 readied and admin-merged at **`640c02de`**; push Publish run 35101406613 cancelled; `tag-and-release.yml platform=all` dispatched (run 35101468982).
- 13:23 — bundle run green → web-bundle `main` at `0472e5b0`, entry `entry-da371738…` (v1.10.29 was `entry-9dabae16…`), worker blob `3824bc10` == monorepo. Tagged **v1.10.30**. `@1.10` read 1.10.29 three times → purged `metadata.json` + `index.html` → serves `x-jsd-version: 1.10.30`; entry asset 200. **Web live.**
- 13:25 — worker vendored onto free branch `chore/revendor-worker-1.10.30` (blob-SHA verified free == bundle == monorepo == `3824bc10`); [woocommerce-pos#2001](https://github.com/wcpos/woocommerce-pos/pull/2001) admin-merged at `340586ee`; `release.yml` re-runs and re-uploads the zip into the held draft.
- 13:28 — free `release.yml` run 35101975737 on `340586ee` "Draft or published release v1.10.16 already exists. Reusing it": zip re-uploaded (8,074,207 B); downloaded and verified: header `Version: 1.10.16`, `Stable tag: 1.10.16`, worker bytes identical to bundle v1.10.30's (`3824bc10`). Note the zip is FLAT (no `woocommerce-pos/` top folder) — a verifier that assumes the folder reads "no such file".
- 13:30 — free **v1.10.16 published** (`gh release edit --draft=false --latest`, body = plugin notes). **Trap: the draft's `target_commitish` was the FIRST push `e82dffc5`, so publishing created the tag there — the commit with the OLD worker — while the asset was built from `340586ee`.** `wporg-deploy.yml` checks out the tag, so run 35102361086 started from `e82dffc5`. Fixed within a minute: tag force-moved to `340586ee` (worker at tag `3824bc10`), run 35102361086 cancelled (it reached the Deploy step; the SVN commit is verified below), `wporg-deploy.yml` re-dispatched with `version=1.10.16` (run 35102462455, head `340586ee`). Lesson: when a draft is reused across pushes, PATCH its `target_commitish` to the final SHA before publishing.
- 13:2x — `main` → `next` API merge 409 on `apps/main/public/opfs.worker.js` (and, in the subagent's worktree, the `apps/main/package.json` version hunk — kept next's 1.11.0); handed to a worktree subagent, which regenerated the bundle with `scripts/build-opfs-worker.mjs` after a fresh install per the 2026-09-15b precedent. Rebuilt worker `696dcf07` is a genuine union: carries `WCPOS_CLEANUP_COMPACTION_BATCH_PATCH` (main only) AND `WCPOS_EXACT_CLEANUP_DELETE_PATCH` (next only). Checks: `node --test scripts/rxdb-premium-cleanup-compaction-batch.test.mjs` 11/11, `pnpm typecheck --force` 15/15. Sync PR [#2109](https://github.com/wcpos/monorepo/pull/2109) (`b7c876ff`), auto-merge queued behind its Merge Gate (a sync into `next` is a real merge, not a version bump — not admin-merged).
- 13:32 — Pro #573 readied and admin-merged at `79783c57`; `manual-release.yml releaseVersion=1.10.16` dispatched (run 35102607144).
- 13:34 — OTA run 35101355561 green. `eas update:list --branch production --json`: Android group `084535ac-99ad-49a5-be6b-3bab8b5b76e9` runtime `050c0144…`, iOS group `584e3e90-8014-45a2-847b-ae42919d8edd` runtime `612a275d…`, message `app v1.10.20 5bb4577d` — equal to the 1.10.18 store builds' runtime versions. **Mobile live over the air.** Electron: Linux and Windows published, macOS x64/arm64 building.
- 13:34 — `wporg-deploy.yml` re-dispatch run 35102462455 (head `340586ee`) green. SVN verified: trunk `Stable tag: 1.10.16`, `tags/1.10.16/` 200, tagged header `Version: 1.10.16`, worker bytes in BOTH `tags/1.10.16/` and `trunk/` identical to bundle v1.10.30's (`3824bc10`) — the cancelled run committed nothing stale. **Free live on WP.org.**
- 13:3x — Pro `manual-release.yml` run 35102607144 green; **Pro v1.10.16 published** (body = CHANGELOG top section). Shipped zip verified: 2 Pro markers at 1.10.16, translations 2026.9.10, vendored free `Stable tag: 1.10.16` / header 1.10.16, worker bytes identical to bundle v1.10.30's (`3824bc10`). The Pro zip is flat too (`woocommerce-pos-pro.php` at the root, free under `vendor/wcpos/woocommerce-pos/`).
